[package]
name = "cmux-ctl"
version = "0.1.0"
edition = "2021"
description = "cmux agent orchestration CLI"

[[bin]]
name = "cmux-ctl"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
